import { RepositoryProvider } from './repository-provider';

describe('RepositoryProvider', () => {
  it('should create an instance', () => {
    expect(new RepositoryProvider()).toBeTruthy();
  });
});
